# AvaxLens Audit — February 26, 2026

> Full-stack audit after v0.4.0 (Transactions, Functions, Callers tabs)

---

## Audit Summary

**Overall Score: 7.5/10**
**Critical:** 0 | **High:** 3 | **Medium:** 4 | **Low:** 3 | **Info:** 3

### Key Findings:
1. Unbounded in-memory cache can grow indefinitely (no max size, no cleanup)
2. No rate limiting on API routes — external API quotas can be exhausted by attackers
3. Missing security headers (CSP, X-Frame-Options, X-Content-Type-Options)

### Verdict: NEEDS FIXES (non-blocking for hackathon demo, fix before production)

---

## Categories Checked

| Category | Status |
|----------|--------|
| Frontend (Next.js) | Checked |
| Backend API (API routes) | Checked |
| Dependencies | Checked |
| Infrastructure & Config | Checked |
| Smart Contracts | Skipped (no .sol files) |
| Database | Skipped (no database) |
| Authentication | Skipped (no auth) |
| Browser Extension | Skipped (not applicable) |
| Telegram Bot | Skipped (not applicable) |
| Tests | Skipped (no test suite) |

---

## Detailed Findings

### [HIGH] #1: Unbounded Cache Growth (Memory Leak)

**Location:** `src/lib/cache.ts:1-15`
**Verified:** Yes — Read confirmed no size limits, no periodic cleanup
**Risk:** In-memory Map grows indefinitely. Each unique address creates multiple cache entries (txs, abi, source, info, analytics per period). Expired entries are only cleaned on read access. An attacker can scan thousands of addresses to exhaust server memory.
**Impact:** Server crash / Vercel function memory limit exceeded
**Fix:**
```typescript
const MAX_CACHE_SIZE = 500;

export function setCache(key: string, data: unknown, ttlMs: number): void {
  if (cache.size >= MAX_CACHE_SIZE) {
    // Evict oldest entry
    const firstKey = cache.keys().next().value;
    if (firstKey) cache.delete(firstKey);
  }
  cache.set(key, { data, expiry: Date.now() + ttlMs });
}
```
**Effort:** S
**Status:** OPEN

---

### [HIGH] #2: No Rate Limiting on API Routes

**Location:** `src/app/api/contract/[address]/analytics/route.ts`, `info/route.ts`, `transactions/route.ts`
**Verified:** Yes — no rate limiting middleware exists in any route
**Risk:** External APIs (Snowtrace, Routescan) have 2 req/sec and 10K/day limits. An attacker can make unlimited requests to your API routes, exhausting the external API quota and degrading service for all users.
**Impact:** Service unavailability due to exhausted API quotas
**Fix:** Add simple in-memory rate limiter or use Vercel's built-in rate limiting. For hackathon, a simple IP-based counter with sliding window would suffice.
**Effort:** M
**Status:** OPEN

---

### [HIGH] #3: Missing Security Headers

**Location:** `next.config.ts:3`
**Verified:** Yes — config is empty: `const nextConfig: NextConfig = {};`
**Risk:** Missing X-Frame-Options (clickjacking), X-Content-Type-Options (MIME sniffing), Referrer-Policy, CSP
**Impact:** Standard web security vulnerabilities
**Fix:**
```typescript
const nextConfig: NextConfig = {
  headers: async () => [
    {
      source: '/:path*',
      headers: [
        { key: 'X-Content-Type-Options', value: 'nosniff' },
        { key: 'X-Frame-Options', value: 'DENY' },
        { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
      ],
    },
  ],
};
```
**Effort:** S
**Status:** OPEN

---

### [MEDIUM] #4: Missing Query Parameter Validation in Transactions Route

**Location:** `src/app/api/contract/[address]/transactions/route.ts:19-21`
**Verified:** Yes — `parseInt` without validation, `sort` without whitelist
**Risk:** `parseInt('abc')` returns `NaN`, negative values pass through, `sort` param is unvalidated. These values are passed to Routescan API URL construction.
**Impact:** Malformed external API requests, unexpected behavior
**Fix:**
```typescript
const page = Math.max(1, parseInt(searchParams.get('page') || '1') || 1);
const offset = Math.min(10000, Math.max(1, parseInt(searchParams.get('offset') || '10000') || 10000));
const validSorts = ['asc', 'desc'];
const sort = validSorts.includes(searchParams.get('sort') || '') ? searchParams.get('sort')! : 'desc';
```
**Effort:** S
**Status:** OPEN

---

### [MEDIUM] #5: Missing Response Schema Validation in TransactionsTab

**Location:** `src/components/dashboard/TransactionsTab.tsx:35-38`
**Verified:** Yes — `json.data.transactions` accessed without Array.isArray check
**Risk:** If API returns unexpected structure, `processRawTransactions()` crashes with unhandled error
**Impact:** Tab shows blank/broken state instead of error message
**Fix:**
```typescript
if (json.success && Array.isArray(json.data?.transactions)) {
  const processed = processRawTransactions(json.data.transactions);
  setTransactions(processed);
} else {
  setError('Failed to load transactions');
}
```
**Effort:** S
**Status:** OPEN

---

### [MEDIUM] #6: No Client-Side Fetch Timeout

**Location:** `src/components/dashboard/TabNavigation.tsx:38`, `src/components/dashboard/TransactionsTab.tsx:30`
**Verified:** Yes — client-side fetch calls use native `fetch()` without timeout, while server-side uses `fetchWithTimeout()`
**Risk:** If backend hangs, user sees indefinite loading spinner
**Impact:** Poor UX; user stuck on loading state
**Fix:** Use `AbortController` with 15s timeout on client-side fetch calls
**Effort:** S
**Status:** OPEN

---

### [MEDIUM] #7: No Test Suite

**Location:** Project-wide
**Verified:** Yes — no `__tests__/`, `*.test.ts`, or `*.spec.ts` files exist
**Risk:** No automated verification of processing logic (analytics, functions, callers, transactions)
**Impact:** Regressions can be introduced without detection
**Fix:** Add unit tests for `src/lib/processing/*.ts` — these are pure functions, easy to test
**Effort:** L
**Status:** OPEN

---

### [LOW] #8: Race Condition on Rapid Period Changes

**Location:** `src/components/dashboard/TabNavigation.tsx:33-54`
**Verified:** Yes — no request cancellation on rapid period switches
**Risk:** If user rapidly switches 7d → 30d → 90d, responses may arrive out of order and display stale data
**Impact:** Wrong period data displayed briefly
**Fix:** Use `AbortController` to cancel previous request when new one starts
**Effort:** S
**Status:** OPEN

---

### [LOW] #9: npm Audit Vulnerabilities (Dev Dependencies Only)

**Location:** `node_modules/ajv`, `node_modules/minimatch`
**Verified:** Yes — `npm audit` shows 2 vulnerabilities
**Risk:** ReDoS in ajv (moderate) and minimatch (high). Both are in dev dependencies only (ESLint toolchain) — not shipped to production.
**Impact:** None in production. Could affect local dev if processing malicious glob patterns (extremely unlikely).
**Fix:** `npm audit fix`
**Effort:** S
**Status:** OPEN

---

### [LOW] #10: Catch Blocks Swallow Errors Silently

**Location:** `src/lib/apis/snowtrace.ts:23,46`, `src/lib/apis/routescan.ts:26,47`
**Verified:** Yes — empty catch blocks with only comments
**Risk:** When external APIs fail, there's no logging — makes debugging production issues very difficult
**Impact:** No observability into API failures
**Fix:** Add `console.error()` in catch blocks for server-side code
**Effort:** S
**Status:** OPEN

---

### [INFO] #11: ComingSoon Component No Longer Used

**Location:** `src/components/ui/ComingSoon.tsx`
**Verified:** Yes — no imports found anywhere in codebase
**Risk:** Dead code
**Impact:** None
**Status:** Can be removed during cleanup

---

### [INFO] #12: Strict Address Validation — Excellent

**Location:** `src/lib/validation.ts:1-3`
**Verified:** Yes — `/^0x[a-fA-F0-9]{40}$/` is strict and correct
**Risk:** None — this is a positive finding
**Impact:** SSRF/injection via address param is not possible
**Status:** PASS

---

### [INFO] #13: No Secrets in Code or Git History

**Verified:** Yes — grep for API_KEY, SECRET, PASSWORD, PRIVATE_KEY, TOKEN found nothing. No `.env` files tracked. All external APIs are free tier, no auth required.
**Status:** PASS

---

## Comparison with Previous Audit (Feb 23, 2026)

| Finding | Previous | Current |
|---------|----------|---------|
| HTTP status validation | HIGH — OPEN | FIXED (res.ok checks added) |
| Fetch timeout | HIGH — OPEN | FIXED (fetchWithTimeout in all API calls) |
| Rate limit protection | HIGH — OPEN | STILL OPEN |
| Wrong cache TTL | MEDIUM — OPEN | FIXED |
| NaN propagation | MEDIUM — OPEN | FIXED (guards in all format functions) |
| Missing tx.from null check | LOW — OPEN | FIXED (in callers.ts) |
| No test suite | MEDIUM — OPEN | STILL OPEN |
| Unbounded cache | Not found | NEW — HIGH |
| Missing security headers | Not found | NEW — HIGH |
| Query param validation | Not found | NEW — MEDIUM |

**Progress:** 5 of 8 previous findings fixed. 3 new findings from expanded codebase.

---

## Action Items (Priority Order)

| # | Action | Severity | Effort | Files |
|---|--------|----------|--------|-------|
| 1 | Add max cache size with eviction | HIGH | S | `cache.ts` |
| 2 | Add security headers to next.config.ts | HIGH | S | `next.config.ts` |
| 3 | Validate query params in transactions route | MEDIUM | S | `transactions/route.ts` |
| 4 | Add Array.isArray check in TransactionsTab | MEDIUM | S | `TransactionsTab.tsx` |
| 5 | Add AbortController timeout to client fetches | MEDIUM | S | `TabNavigation.tsx`, `TransactionsTab.tsx` |
| 6 | Add console.error to catch blocks | LOW | S | `snowtrace.ts`, `routescan.ts` |
| 7 | Run npm audit fix | LOW | S | `package-lock.json` |
| 8 | Add rate limiting | HIGH | M | API routes |
| 9 | Add unit tests for processing functions | MEDIUM | L | `processing/*.ts` |

Items 1-7 can be done in one session (~30 min). Rate limiting (8) and tests (9) are larger efforts.

---

## Metrics

| Metric | Value | Target | Status |
|--------|-------|--------|--------|
| Production build | PASS | PASS | OK |
| TypeScript strict mode | Enabled | Enabled | OK |
| npm audit (production) | 0 vulns | 0 | OK |
| npm audit (dev) | 2 vulns | 0 | WARN |
| Test coverage | 0% | >80% | FAIL |
| Security headers | 0 | 3+ | FAIL |
| Input validation | Strong | Strong | OK |
| Secrets in code | None | None | OK |

---

## Verification Summary

All findings were independently verified by reading the actual source code:

| Finding | Source | Verification |
|---------|--------|-------------|
| #1 Cache growth | API agent | Verified — Read cache.ts, confirmed no size limit |
| #2 Rate limiting | API agent + Infra agent | Verified — Read all route.ts files, no middleware |
| #3 Security headers | Infra agent | Verified — Read next.config.ts, empty config |
| #4 Query params | API agent | Verified — Read transactions/route.ts:19-21 |
| #5 Schema validation | Frontend agent | Verified — Read TransactionsTab.tsx:35-38 |
| #6 Fetch timeout | Frontend agent | Verified — Read TabNavigation.tsx:38 |
| #7 No tests | Infra agent | Verified — Glob found no test files |
| #8 Race condition | Frontend agent | Verified — Read TabNavigation.tsx:33-54 |
| #9 npm vulns | npm audit | Verified — Both in devDependencies only |
| #10 Silent catches | API agent | Verified — Read snowtrace.ts:23,46; routescan.ts:26,47 |
| #11 Dead code | Grep | Verified — No imports of ComingSoon found |
| #12 Validation | All agents | Verified — Read validation.ts, strict regex |
| #13 No secrets | Infra agent | Verified — Grep found nothing |

**False positives discarded:**
- API agent reported "CRITICAL: Unbounded cache growth" — Downgraded to HIGH. On Vercel serverless, functions are ephemeral (cache resets on cold start). Risk is real but mitigated by platform. Still should fix.
- API agent reported cache key collision risk — Discarded as false positive. Address validation (`/^0x[a-fA-F0-9]{40}$/`) prevents `:` in addresses.
- Infra agent reported "Rate limit protection" as calling APIs in parallel — Verified as FALSE. `analytics/route.ts:41-45` shows sequential calls (`await` one after another), not parallel.
